<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorBasicButton</item>

    </style>

    <style name="Button" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/colorBasicButton</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:textSize">32sp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:onClick">onKeyPress</item>
    </style>

    <style name="ButtonControl" parent="Button">
        <item name="android:background">@color/colorControlButton</item>
        <item name="android:textSize">24sp</item>
    </style>

    <style name="ButtonScientific" parent="ButtonControl">
        <item name="android:background">@color/colorScientificButton</item>
    </style>

</resources>
